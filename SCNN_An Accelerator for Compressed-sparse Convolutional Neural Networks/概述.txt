课题：也是针对稀疏的CNN来进行加速
特点：
（1）对input feature 和 weight中为0的都跳过计算，即只计算weight和input feature中同时都不为0的相乘。
（2）这篇文章创新在于对weight和input feature都引入了坐标，因此可以通过坐标直接计算出对应的psum存放位置。
（3）因为PE的规模都不大，这篇文章将数据进行了切图。每个PE之间可以进行数据交换和沟通，交换的是halos（边界数据）。
缺点：
        文章没有提到面积的问题，这种设计的面积应该也会很大