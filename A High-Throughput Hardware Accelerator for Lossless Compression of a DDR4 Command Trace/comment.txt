介绍了一种高吞吐率的Huffman编码器

首先这种编码器面向的是DDR4 command来编码，不是对需要计算的数据编码

方法是主要是通过将数据分块，然后用多个Huffman编码器同时进行压缩。
这样的好处是减少了每个码表的最长符号的码长，且可以并行编码，但是整体的压缩率降低
->因此这篇文章对数据进行的预处理，用了两种方法：1、Don’t Care Bits Override 2、Bits Arrange

但是以上的两种方法目的都是降低command的熵，因此这两种方法也只对command数据才有用，对于别的数据如果通过分块的方法进行处理
会使得压缩率变低。